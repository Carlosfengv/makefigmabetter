# 首版能力兼容矩阵

状态定义：`Supported` 已完成闭环；`Partial` 存在明确限制；`Later` 已规划但尚未实现；`Unsupported` 不在产品范围；`N/A` 表示该兼容维度不适用。`pnpm check:compatibility` 会校验每一行均有四个明确维度状态和当前限制说明。

| 能力 | Schema | 视觉 | 编辑行为 | Round-trip | 当前状态 |
| --- | --- | --- | --- | --- | --- |
| Document / Page / SceneNode 层级 | Supported | N/A | Partial | Supported | Core、WASM Snapshot v15 与浏览器投影已保存稳定 DocumentId、PageId、NodeId、parent_id、Canonical PositionId、Resource Index、节点级图片 `AssetId`（Image、Frame、Rectangle、Ellipse）与可选 Canonical TextProperties；旧 v1–v9 Snapshot 单向迁移到 Page 1，v10 单向补齐默认 DocumentId，v12 加入 AssetId/contentHash 元数据，v13 加入节点级图片引用，v14 加入文本样式，v15 固化图片填充操作。页面可创建、切换、保存和恢复；Section/Group/Line 和嵌套图层编辑在 Phase 2 完成 |
| Frame / Rectangle / Ellipse / Text | Partial | Partial | Partial | Partial | 基础单机原型；rotation、stroke 与 stroke width 已进入 Core Snapshot v9、Operation hash 与 Undo/Redo；当前节点尚未形成完整的容器嵌套编辑闭环 |
| Text 内容与排版 | Partial | Partial | Partial | Supported | Canonical TextProperties 已以 FontId/face index/variation axis、UTF-8 scalar 边界 Style Runs、段落对齐/行高/间距、Auto Size 和 Fallback Chain 进入 Core、Protobuf、WASM Snapshot v14、Undo/Redo 与远端 Operation；Inspector 可编辑基础样式，组合输入期间保留草稿、仅在 composition commit 时提交原子更新。Canvas 投影按 UTF-8 range 逐 run 应用字号、字重、斜体、字距与已加载字体；当文本为单一显式 FontId Run 且 FontFace 已就绪时，Worker 采用 Rust `graphics-core` 的 ICU4X 断行 + Rustybuzz（HarfBuzz 兼容）advance 生成并校验 UTF-8 行范围，直接驱动画布换行，缩放不会改变行范围。每个 Rust 行还会以 UAX #9 level run 的显示顺序输出未改写的 UTF‑8 byte range，并依各 run 方向 shaping；Canvas 用作复杂文本的绘制过渡层。左对齐、无旋转、400 字重、无斜体、无字距与无段落间距的非变量 LTR 单 run 还可从显式 OpenType outline 生成有 512px 上限、固定 4×4 采样的 alpha 字形位图，并通过实际 WebGPU Text Pass 写入一张固定 1024×1024、带 1px padding 的 R8 GPU Glyph Atlas；图集在同一 Device generation 内复用，无法装入的节点整体降级 Canvas，Device Lost 后由同一可重建位图重新上传。混合 Style Run、缺失字体、可变字体、其他字重/样式、RTL GPU 绘制和超过 4,096 glyph 的段落仍显式降级为浏览器 Canvas/Bidi 过渡路径。画布双击文字会临时启用浏览器原生 textarea，因此支持原生 Caret、选区和 IME；Esc 取消，失焦或 ⌘/Ctrl+Enter 以原子 Text Transaction 提交。Rust 同时暴露不拆分字素簇的 Caret Map、选区替换/输入法预编辑结果，以及可在 Device Lost 后由字体字节重建的确定性 Glyph Atlas 位置分配与预算；这些运行时状态不写入 Canonical Document。FreeType 栅格、字符镜像/复杂脚本的完整视觉细节、Rust Caret Map 与画布编辑器位置的一一对应、多页/淘汰策略的 GPU Glyph Atlas 和复杂文本 GPU 排版仍在 Phase 1 |
| 无限画布、平移与缩放 | N/A | Partial | Partial | N/A | 基础单机原型 |
| 变换、基础命中与几何数值 | Partial | Partial | Partial | N/A | Rust f64 仿射/Bezier/Bounds/Fill/Stroke Spike；Canvas 投影支持旋转、椭圆、圆角命中，Worker Spatial Grid 已用于候选查询与视口裁剪；任意 Path Boolean、Clip/Mask、深层选择、生产级 R-tree/BVH，以及八方向和多选 Transform 在 Phase 2 完成 |
| 命令、Operation、幂等与 Undo/Redo | Supported | N/A | Partial | Partial | Rust 语义 HistoryItem、transactionId 与 operationId 幂等、Envelope payload SHA-256、PositionId Canonical 排序与确定性本地回放已完成；Protobuf 契约、Document Service 的可信 Principal 注入、对象级 ACL、accepted revision 与重启回放已落地。浏览器会持久化 opaque pending Envelope、按序发送、处理 Ack、断线重试并在冲突时以服务端 Snapshot 对账；多人合并与协同 Undo 仍在 Phase 4 |
| 本地快照恢复与多标签页 | Partial | Partial | Partial | Partial | OPFS Core Snapshot + IndexedDB Journal/Manifest 回退；支持 Web Locks 时以单写者与带优先级的 BroadcastChannel 交接保证唯一 Owner，刷新同步释放 Lease；没有 Web Locks 时保留本地编辑但不保证多标签一致性。未处理 Engine Worker 异常可从最近确认快照有界重启，连续失败进入安全模式；浏览器与服务端之间的 pending Operation Ack/重试/远端 Snapshot 对账已完成，跨浏览器多人协调属于 Phase 4 |
| WebGPU / WebGL2 | Partial | Partial | N/A | N/A | Worker 使用辅助 OffscreenCanvas 的实际 WebGPU/WGSL Scene Renderer 绘制实色 Frame/Rectangle/Ellipse，并合成至 Canvas 2D 的网格、渐变和不支持文本覆盖层；已解码 ImageBitmap 以独立 Image Pass 按 AssetId 建立、复用和回收有界 GPU Texture，再按节点的 cover-crop UV、旋转和透明度绘制；未解码图片仍保留 Canvas 占位。单显式字体 LTR 文本从 Rust 字形 alpha mask 写入一张有预算的 1024×1024 R8 Text Atlas，再以 atlas UV 绘制；其余文本保持 Canvas。Device Lost 最多恢复一次。Rust `renderer-wgpu` 已定义不持有 Document/GPU Handle 的 Dirty Set → Scene → Culling → 固定 Main/Image/Text/Overlay/Composite Pass 计划；WASM 会按 active Page 导出经过验证的计划，Worker 只在 Canonical 投影边界加载它、每帧仍以空间索引裁剪，并以该计划排列 Canvas 回退的 Shape/Image/Text 绘制顺序；拖拽中的瞬态场景安全回退为本地投影。当前 Canonical revision 的实色形状还会投影为已验证的 16-float 实例批次，由 Worker 直接上传到同一 WebGPU/WGSL executor。`renderer-wgpu` 还新增了仅原生目标的 `native-wgpu-executor` 特性：它实际持有 Rust `wgpu` 的 Buffer/Uniform/Pipeline/Queue，在 executor-owned、按尺寸复用且受预算限制的离屏 TextureView 上按 MainScene → Image → Text → Overlay → Composite 顺序提交受限 Pass；Composite 使用全屏采样复制到独立输出 TextureView，Overlay 仅接收瞬态选择/辅助图元，图片 Texture 按短生命周期已验证像素输入的内容键复用，并以 source/target aspect ratio 计算居中的 cover-crop UV；文字 alpha mask 进入单张带 padding 的 1024×1024 R8 Glyph Atlas，并按 glyph key 复用。`WgpuExecutorFactory` 可在平台报告 Device Lost 后单次请求新的 Device/Queue/executor，由最新 Render Graph 重放；实际失效通知接线尚未进入浏览器主路径。它尚未被浏览器主路径接管，也尚未实现图片图集、复杂文本/RTL GPU 绘制或多页 glyph atlas。当前 WGSL 为 encoded-sRGB、non-premultiplied source-alpha 合约，Rust 输出已逐字节对齐。高级 Mask/Blur/Blend Effect 在 Phase 2 完成 |
| 色彩空间、alpha 与渐变插值 | Partial | Partial | Partial | Partial | Rust Core 已固定 sRGB / Display P3 / Linear sRGB、非预乘存储、DocumentColorProfile，以及带 2–16 个显式色彩 stop 的 Canonical LinearGradient；Snapshot v9 保留 `colorProfile`/`fillColor`/`fillGradient`/`strokeColor`/`strokeGradient`/`rotation`/`stroke`/`strokeWidth` 并迁移旧 CSS Fill 与 sidecar appearance。Canvas 2D 以与 Core 相同的 P3→sRGB 回退和每区间 64 个线性 sRGB 样本近似绘制填充及描边，WebGPU 过渡层把渐变 Paint 留给 Canvas 覆盖层；Canvas 2D 广色域、其他渐变类型、图片 ICC 与导出后续完成 |
| Golden、性能采样与诊断证据 | Partial | Partial | Partial | N/A | 固定 F-PHASE0-BASIC-CARD Fixture、受限 Worker 诊断日志、滚动 P50/P95/最大值采样和验收模板已就绪；首张 Canvas 2D / 1440×960 / DPR 1 的经审核 PNG Golden 已冻结，后续采集按 SHA-256 严格比对。Phase 1 的 `F-TEXT-MULTILINGUAL` 可直接打开，覆盖混合 Style Run、中日韩、RTL、Indic、Emoji ZWJ 和组合字符，并在导入前校验 UTF-8 run 边界；`F-TEXT-10K` 同样以 SHA-256 固定，确认 10,000 UTF-8 字节完整文本与单一完整 Style Run，可通过 `?fixture=phase1-text-10k` 直接进行 Canvas/WebGPU 性能采样。`F-SHAPE-100K` 以版本化配方、固定随机种子与精确节点分布生成，避免静态巨型 JSON；可通过 `?fixture=phase1-shape-100k` 直接压测 100,000 节点的加载、裁剪、缩放与 Render Graph 排序。`F-PHASE1-RENDER-COMPOSITE` 固定一个经 SHA-256、PNG IHDR CRC 和尺寸校验的图像资源，以及 Frame / Image cover crop / Ellipse / Gradient / Text 同页组合，覆盖 Resource Index、WASM、Worker decode 与 WebGPU Image Pass 的输入边界。该基准投影仍先写入 Rust Core，但不创建无用的完整本地 Snapshot；本机实测 WASM heap 为 215.1 MiB（256 MiB 预算内）。Phase 1.11 还以真实 HTTP 适配器验证了 Document 的 SQLite 重开、Snapshot 读取与 Operation 幂等重放，以及 Asset 的 SQLite 重开、文档级授权和字节保真下载；浏览器脚本也验证了一次 WebGPU 恢复和两次丢失后的稳定 Canvas 回退，两个场景均无控制台错误。图片 Golden 的独立人工审核仍在 Phase 1 |
| 文档、Undo/Operation 与渲染表面预算 | Partial | N/A | Partial | N/A | 100,000 节点与 256 MiB Canonical Node 估算准入、已分配 WASM linear memory 的 256 MiB 软诊断、Core 历史/去重预算、512 MiB Canvas backing surface 预检已实现；Asset Probe 还以 256 MiB 在途总量预算限制并发探测，完成/取消会释放预留。实际 WebGPU Scene 在配置前也估算三张 RGBA8 swap-chain 表面和顶点缓冲，以 256 MiB 硬上限回退 Canvas 而不改 Document，并显示瞬态资源指标。Rust `renderer-wgpu` 的资源池现已固定预算准入、替换不破坏旧资源、释放、Device Lost 清空、一次重建和二次显式 Canvas 降级的语义；其原生 `wgpu` Main/Image/Text executor 也在实际 Buffer、Image Texture 和单页 Glyph Atlas 创建前按同一 256 MiB 上限准入，拒绝不改变 Graph/Document。离屏池与完整 Device 重建仍在 Phase 1 |
| 对象级 AuthZ 与不可信资源 | Partial | Partial | Partial | Partial | Core AuthZ Contract Harness 覆盖 tenant/document/editor/actor 注入并产生 v1 脱敏授权审计事件；独立 Asset Probe Worker 对 SVG、PNG/JPEG/WebP 与 WOFF/WOFF2/TTF/OTF 做有界 MIME/头信息识别，并为接受、拒绝或取消生成 v1 脱敏事件。`F-ASSET-HOSTILE` 固定了 Script SVG、外部引用、伪造 MIME、截断 PNG、解码放大 PNG 与可识别 WOFF2 容器，逐项重放隔离探测/准入结果。浏览器导入经 Worker 预检后，SQLite Asset Service 以服务端 offset 续传、验证内容 Hash/MIME、同租户去重、Document writer 附加与 reader + 短期随机凭据下载；`RegisterResource` Operation 将 AssetId/contentHash/MIME/尺寸写入 Core、Protobuf/WASM 和远端 Snapshot，随后将图片应用到已选中的 Frame / Rectangle / Ellipse / Image 图层填充，未选中可填充图层时才创建可恢复渲染的 Image 节点。上传可由用户取消，Worker 的 decoded ImageBitmap 使用 256 MiB LRU CPU 缓存；原始资源在可用时写入 128 MiB OPFS LRU 缓存，并按 content hash 复核后使用。导入字体可作为 Canonical FontReference 被文本节点引用，经 Worker FontFace 加载并在失败时可见地降级为系统字体；完整隔离解码、字形塑形和审计投递仍在 Phase 1 |
| 完整编辑语义与可访问 UI | Later | Later | Later | Later | Section、Group、Line、嵌套图层树、跨父级排序、八方向与多选 Transform、Shadow、Mixed Inspector、Menu/Dialog/Tooltip/Toast 和完整键盘/屏幕阅读器流程统一归入 Phase 2 |
| 前端目录、UI Primitive 与构建边界 | Partial | N/A | Partial | N/A | App Router、`components.json`、`components/ui`、ESLint 导入限制、依赖 allowlist、Rust 工具链锁定与 CI 已就绪；`services/mock-backend` 已作为独立进程及版本化契约检查存在，Phase 1 建立可独立构建、部署和回滚的 Rust Document/Asset Backend |
| Auto Layout、Constraints | Later | Later | Later | Later | Phase 2 |
| Components、Variables、Figma 导入/写回 | Later | Later | Later | Later | Phase 3 |
| 多人协同、评论、权限 | Later | N/A | Later | N/A | Phase 4 |
| `.fig` 私有文件读写 | Unsupported | Unsupported | Unsupported | Unsupported | 明确非目标 |
