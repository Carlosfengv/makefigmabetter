# 首版能力兼容矩阵

状态定义：`Supported` 已完成闭环；`Partial` 存在明确限制；`Later` 已规划但尚未实现；`Unsupported` 不在产品范围；`N/A` 表示该兼容维度不适用。`pnpm check:compatibility` 会校验每一行均有四个明确维度状态和当前限制说明。

| 能力 | Schema | 视觉 | 编辑行为 | Round-trip | 当前状态 |
| --- | --- | --- | --- | --- | --- |
| Document / Page / SceneNode 层级 | Supported | N/A | Partial | Supported | Core、WASM、Snapshot、Operation 与浏览器投影保持稳定的 DocumentId、PageId、NodeId、parent_id、Canonical PositionId、资源索引与节点属性；Group、Line、Section、Relative-v1 Transform 与跨父级拖拽均已进入闭环。编辑维度仍为 Partial，因为完整多人并发与协同 Undo 明确属于 Phase 4。 |
| Frame / Rectangle / Ellipse / Text | Supported | Partial | Partial | Partial | Frame、Rectangle、Ellipse 与 Text 已具备创建、嵌套、Resize、基础 Inspector、命中、保存、Undo/Redo 和 SVG 导出；Frame 支持 clipsContent 与约束，完整 Ellipse 支持 Inside/Center/Outside Stroke Align，Ellipse 还支持 Arc/Donut（Arc/Donut 仅 Inside Stroke）。复杂 Text 视觉、完整 Auto Layout、非 Vector 节点的 Paint Outline 与逐像素跨渲染器一致性仍为明确降级。 |
| Text 内容与排版 | Partial | Partial | Partial | Supported | Canonical TextProperties 已以 FontId/face index/variation axis、UTF-8 scalar 边界 Style Runs、段落对齐/行高/间距、Auto Size 和 Fallback Chain 进入 Core、Protobuf、WASM Snapshot v14、Undo/Redo 与远端 Operation；Inspector 可编辑基础样式，并以 `wght=650, wdth=92` 的受校验语法写入至多 16 个按 tag 排序的 Variable Font 坐标；组合输入期间保留草稿、仅在 composition commit 时提交原子更新。Canvas 投影按 UTF-8 range 逐 run 应用字号、字重、斜体、字距、已加载字体及安全规范化的 `font-variation-settings`；SVG tspan 使用同一轴值，PNG/PDF 消费该冻结 SVG。单一显式 FontId Run 且 FontFace 已就绪时，Worker 采用 Rust `graphics-core` 的 ICU4X 断行 + Rustybuzz（HarfBuzz 兼容）advance 生成并校验 UTF-8 行范围，直接驱动画布换行，缩放不会改变行范围。每个 Rust 行还会以 UAX #9 level run 的显示顺序输出未改写的 UTF‑8 byte range，并依各 run 方向 shaping；Canvas 用作复杂文本的绘制过渡层。左对齐、无旋转、400 字重、无斜体、无字距与无段落间距的 LTR 单 run（包括经排序的 Variable Font axis）可从显式 OpenType outline 生成有 512px 上限、固定 4×4 采样的 alpha 字形位图，并通过实际 WebGPU Text Pass 写入最多四页、每页 1024×1024 且带 1px padding 的 R8 GPU Glyph Atlas；图集在同一 Device generation 内复用，页满时仅在帧间替换未被当前帧引用的 LRU 页，无法安全替换时节点整体降级 Canvas，Device Lost 后由同一可重建位图重新上传。混合 Style Run、缺失字体、其他字重/样式、RTL GPU 绘制和超过 4,096 glyph 的段落仍显式降级为浏览器 Canvas/Bidi 过渡路径。画布双击文字会临时启用原生 `contentEditable` 作为输入法宿主；DOM UTF-16 光标会经 Engine Worker 请求 Rust/WASM Caret Map 并按相同的“最近且同距取较早 byte offset”规则回写到合法字素簇边界。Esc 取消，失焦或 ⌘/Ctrl+Enter 以原子 Text Transaction 提交。Rust 同时暴露选区替换/输入法预编辑结果，以及可在 Device Lost 后由字体字节重建的确定性 Glyph Atlas 位置分配与预算；这些运行时状态不写入 Canonical Document。正式 Rasterizer 已由 ADR 0021 冻结为 Rust OpenType outline + 固定 supersampling，而非 FreeType；字符镜像/复杂脚本的完整视觉细节、Rust Caret Map 与画布编辑器视觉坐标的一一对应和复杂文本 GPU 排版仍在 Phase 1 |
| 无限画布、平移与缩放 | N/A | Partial | Partial | N/A | 基础单机原型 |
| 变换、基础命中与几何数值 | Supported | Partial | Partial | N/A | Relative-v1 2×3 Affine、旋转/斜切/镜像投影、精确 Line 起点旋转、Frame Clip 命中、八方向单选/多选 Resize、Shift 锁比例、Alt/Option 中心缩放、Line 双端点编辑和 Group 子树缩放均已覆盖；所有常见节点的单选和多选无修饰键 Resize 都允许穿越对侧锚点，以正尺寸和反射矩阵完成镜像。Frame 的约束与 Clip 在反射后仍由同一 Core Transaction/Canvas 裁剪路径保持对子树生效。Vector-only Boolean、同级 alpha Mask 及其基本命中已经接入；任意异构 Path Boolean、像素级选择 bounds 裁剪和生产级 R-tree/BVH 仍未完成。 |
| 命令、Operation、幂等与 Undo/Redo | Supported | N/A | Partial | Partial | Rust 语义 HistoryItem、transactionId 与 operationId 幂等、Envelope payload SHA-256、PositionId Canonical 排序与确定性本地回放已完成；Protobuf 契约、Document Service 的可信 Principal 注入、对象级 ACL、accepted revision 与重启回放已落地。浏览器会持久化 opaque pending Envelope、按序发送、处理 Ack、断线重试；Conflict 时 Worker 从服务端 Snapshot 重放具体的本地意图、为替换 Envelope 分配新 operation/transaction ID，并仅在 IndexedDB 原序列可原子交换后继续发送。重复 PositionId 会重排而保留节点 ID；未知旧记录和临时封装失败会保守阻塞，永久拒绝显示服务端安全诊断。多人合并与协同 Undo 仍在 Phase 4 |
| 本地快照恢复与多标签页 | Partial | Partial | Partial | Partial | OPFS Core Snapshot + IndexedDB Journal/Manifest 回退；支持 Web Locks 时以单写者与带优先级的 BroadcastChannel 交接保证唯一 Owner，刷新同步释放 Lease；没有 Web Locks 时保留本地编辑但不保证多标签一致性。未处理 Engine Worker 异常可从最近确认快照有界重启，连续失败进入安全模式；浏览器与服务端之间的 pending Operation Ack/重试/远端 Snapshot 对账已完成，跨浏览器多人协调属于 Phase 4 |
| WebGPU / WebGL2 | Partial | Partial | N/A | N/A | Worker 使用辅助 OffscreenCanvas 的实际 WebGPU/WGSL Scene Renderer 绘制实色 Frame/Rectangle/Ellipse；完整 Ellipse 与 uniform Stroke/圆角 Frame/Rectangle 的 Center/Outside Stroke Align 由 Rust 预计算实例与 GPU 共享扩展 Quad 和环形模型，Arc/Donut、图片填充、四边 Weight、独立圆角与 Corner Smoothing 仍安全降级 Canvas，并合成至 Canvas 2D 的网格、渐变和不支持文本覆盖层；已解码 ImageBitmap 以独立 Image Pass 按 AssetId 建立、复用和回收有界 GPU Texture，再按节点的 cover-crop UV、旋转和透明度绘制；未解码图片仍保留 Canvas 占位。单显式字体 LTR 文本从 Rust 字形 alpha mask 写入有预算的最多四页 1024×1024 R8 Text Atlas；页满时仅在帧间替换未被当前帧引用的 LRU 页，其余文本保持 Canvas。浏览器 Worker 直接监听当前 `GPUDevice.lost`：第一次失效仅丢弃派生缓存、从最新不可变投影创建新 Device/renderer；连续第二次失效稳定降级 Canvas/WebGL，不修改 Document 或 pending Operation。开发期 `simulateGpuLoss` 会先销毁真实当前 Device，再复用同一失效处理入口，因此 Chromium 未及时兑现第二次 `device.lost` Promise 时也能验证有界回退。Rust `renderer-wgpu` 已定义不持有 Document/GPU Handle 的 Dirty Set → Scene → Culling → 固定 Main/Image/Text/Overlay/Composite Pass 计划；WASM 会按 active Page 导出经过验证的计划，Worker 只在 Canonical 投影边界加载它、每帧仍以空间索引裁剪，并以该计划排列 Canvas 回退的 Shape/Image/Text 绘制顺序；拖拽中的瞬态场景安全回退为本地投影。当前 Canonical revision 的实色形状还会投影为已验证的 16-float 实例批次，由 Worker 直接上传到同一 WebGPU/WGSL executor。`renderer-wgpu` 还新增了仅原生目标的 `native-wgpu-executor` 特性：它实际持有 Rust `wgpu` 的 Buffer/Uniform/Pipeline/Queue，在 executor-owned、按尺寸复用且受预算限制的离屏 TextureView 上按 MainScene → Image → Text → Overlay → Composite 顺序提交受限 Pass；Composite 使用全屏采样复制到独立输出 TextureView，Overlay 仅接收瞬态选择/辅助图元，图片 Texture 按短生命周期已验证像素输入的内容键复用，并以 source/target aspect ratio 计算居中的 cover-crop UV；文字 alpha mask 进入最多四页、带 padding 的 1024×1024 R8 Glyph Atlas，并按 glyph key 复用。`WgpuExecutorFactory` 可在平台报告 Device Lost 后单次请求新的 Device/Queue/executor，由最新 Render Graph 重放。浏览器仍未由 Rust `wgpu` executor 主路径接管，也尚未实现复杂文本/RTL GPU 绘制；Mask、Blend 和除无 spread 单 Drop Shadow 外的 Blur 当前稳定回退 Canvas。该 Drop Shadow 已以两张 128 MiB 上限、总计 256 MiB 的 RGBA8 pool texture 执行 source→blur→premultiplied composite，预算拒绝时保留完整 Canvas 图层。当前 WGSL 为 encoded-sRGB、non-premultiplied source-alpha 合约，Rust 输出已逐字节对齐。 |
| 色彩空间、alpha 与渐变插值 | Partial | Partial | Partial | Partial | Rust Core 已固定 sRGB / Display P3 / Linear sRGB、非预乘存储、DocumentColorProfile，以及带 2–16 个显式色彩 stop 的 Canonical LinearGradient；Snapshot v9 保留 `colorProfile`/`fillColor`/`fillGradient`/`strokeColor`/`strokeGradient`/`rotation`/`stroke`/`strokeWidth` 并迁移旧 CSS Fill 与 sidecar appearance。Canvas 2D 以与 Core 相同的 P3→sRGB 回退和每区间 64 个线性 sRGB 样本近似绘制填充及描边，WebGPU 过渡层把渐变 Paint 留给 Canvas 覆盖层；图片会在隔离 Worker 中按 EXIF 归一并要求浏览器解码器转换为 sRGB，同时仅记录有界的 sRGB/嵌入 ICC 来源标记，不把不可信 profile bytes 写进 Document。Canvas 2D 广色域、其他渐变类型、图片 profile 的 Canonical Schema 与导出仍后续完成 |
| Golden、性能采样与诊断证据 | Partial | Partial | Partial | N/A | 固定 F-PHASE0-BASIC-CARD Fixture、受限 Worker 诊断日志、滚动 P50/P95/最大值采样和验收模板已就绪；首张 Canvas 2D / 1440×960 / DPR 1 的经审核 PNG Golden 已冻结，后续采集按 SHA-256 严格比对。Phase 1 的 `F-TEXT-MULTILINGUAL` 可直接打开，覆盖混合 Style Run、中日韩、RTL、Indic、Emoji ZWJ 和组合字符，并在导入前校验 UTF-8 run 边界；`F-TEXT-10K` 同样以 SHA-256 固定，确认 10,000 UTF-8 字节完整文本与单一完整 Style Run，可通过 `?fixture=phase1-text-10k` 直接进行 Canvas/WebGPU 性能采样。`F-SHAPE-100K` 以版本化配方、固定随机种子与精确节点分布生成，避免静态巨型 JSON；可通过 `?fixture=phase1-shape-100k` 直接压测 100,000 节点的加载、裁剪、缩放与 Render Graph 排序。`F-PHASE1-RENDER-COMPOSITE` 固定一个经 SHA-256、PNG IHDR CRC 和尺寸校验的图像资源，以及 Frame / Image cover crop / Ellipse / Gradient / Text 同页组合，覆盖 Resource Index、WASM、Worker decode 与 WebGPU Image Pass 的输入边界。该基准投影仍先写入 Rust Core，但不创建无用的完整本地 Snapshot；本机实测 WASM heap 为 215.1 MiB（256 MiB 预算内）。Phase 1.11 还以真实 HTTP 适配器验证了 Document 的 SQLite 重开、Snapshot 读取与 Operation 幂等重放，以及 Asset 的 SQLite 重开、文档级授权和字节保真下载；浏览器脚本也验证了一次 WebGPU 恢复和两次丢失后的稳定 Canvas 回退，两个场景均无控制台错误。图片 Golden 的独立人工审核仍在 Phase 1 |
| 文档、Undo/Operation 与渲染表面预算 | Partial | N/A | Partial | N/A | 100,000 节点与 256 MiB Canonical Node 估算准入、已分配 WASM linear memory 的 256 MiB 软诊断、Core 历史/去重预算、512 MiB Canvas backing surface 预检已实现；Asset Probe 还以 256 MiB 在途总量预算限制并发探测，完成/取消会释放预留。实际 WebGPU Scene 在配置前也估算三张 RGBA8 swap-chain 表面和顶点缓冲，以 256 MiB 硬上限回退 Canvas 而不改 Document，并显示瞬态资源指标。Rust `renderer-wgpu` 的资源池现已固定预算准入、替换不破坏旧资源、释放、Device Lost 清空、一次重建和二次显式 Canvas 降级的语义；其原生 `wgpu` Main/Image/Text executor 也在实际 Buffer、Image Texture 和单页 Glyph Atlas 创建前按同一 256 MiB 上限准入，拒绝不改变 Graph/Document。离屏池与完整 Device 重建仍在 Phase 1 |
| 对象级 AuthZ 与不可信资源 | Partial | Partial | Partial | Partial | Core AuthZ Contract Harness 覆盖 tenant/document/editor/actor 注入并产生 v1 脱敏授权审计事件；独立 Asset Probe Worker 对 SVG、PNG/JPEG/WebP 与 WOFF/WOFF2/TTF/OTF 做有界 MIME/头信息识别，并为接受、拒绝或取消生成 v1 脱敏事件。随后的短生命周期 Raster Decode Worker 再次验证容器尺寸、归一 EXIF、要求 sRGB 解码，并以 256 MiB decoded-pixel 上限关闭迟到或取消的 ImageBitmap。`F-ASSET-HOSTILE` 固定了 Script SVG、外部引用、伪造 MIME、截断 PNG、解码放大 PNG 与可识别 WOFF2 容器，逐项重放隔离探测/准入结果。浏览器导入经 Worker 预检后，SQLite Asset Service 以服务端 offset 续传、验证内容 Hash/MIME、同租户去重、Document writer 附加与 reader + 短期随机凭据下载；`RegisterResource` Operation 将 AssetId/contentHash/MIME/尺寸写入 Core、Protobuf/WASM 和远端 Snapshot，随后将图片应用到已选中的 Frame / Rectangle / Ellipse / Image 图层填充，未选中可填充图层时才创建可恢复渲染的 Image 节点。上传可由用户取消，Worker 的 decoded ImageBitmap 使用 256 MiB LRU CPU 缓存；原始资源在可用时写入 128 MiB OPFS LRU 缓存，并按 content hash 复核后使用。导入字体可作为 Canonical FontReference 被文本节点引用，经 Worker FontFace 加载并在失败时可见地降级为系统字体；复杂字体的跨端 shaping/编辑验证仍在 Phase 1 |
| Group | Partial | Partial | Partial | Partial | Group/Ungroup、子树删除、跨父级移动和最后 child 自动解散已有实现。图层类型复核发现 Core 仍可接受事务结束后的空 Group，且 Relative-v1 Group 的 child 单独更新后派生 Bounds 可能保持旧值；精确命中是“只命中 children”还是“空白 Bounds 也可选 Group”也尚未冻结。关闭这些回归前不得标为 Supported。 |
| Line / Arrow | Supported | Partial | Partial | Partial | Line 零高度几何、Arrow 的 `Line + StrokeCap` 映射、旋转选择框、端点编辑、Cap/Join/Dash/Align/装饰端点和 Canvas/SVG 路径已有实现。最短长度钳制时尚未保证固定端点世界坐标不动；WebGPU 对未覆盖 Stroke 组合继续安全降级 Canvas，服务恢复 Golden 仍待验收。 |
| Section | Supported | Partial | Partial | Partial | Section 的创建、嵌套、contentsHidden、标题、Resize、Layers/Inspector、Snapshot/Operation 和 SVG 路径已有实现；完整视觉 Golden、服务恢复与独立可访问性验收仍待冻结。 |
| Polygon | Partial | Partial | Partial | Partial | ADR 0026 冻结 point count、方向与 Resize 语义；参数 NodeKind 已贯通 Schema、Core、WASM、Canvas、Hit Test、SVG、PNG/PDF、创建工具与 Inspector。Rust Core 派生 Fill 轮廓和 Stroke mesh，并由同一 Convert to Vector 原子事务替换参数节点；旋转、镜像与 Undo/Redo 浏览器候选已验证。跨渲染器 Golden 与独立审核尚未完成。 |
| Star | Partial | Partial | Partial | Partial | ADR 0026 冻结 point count、inner ratio、方向与 Resize 语义；参数 NodeKind 已贯通 Schema、Core、WASM、Canvas、Hit Test、SVG、PNG/PDF、创建工具与 Inspector。Rust Core 派生 Fill 轮廓和 Stroke mesh，并由同一 Convert to Vector 原子事务替换参数节点；嵌套 Star 的旋转、镜像与 Undo/Redo 浏览器候选已验证。跨渲染器 Golden 与独立审核尚未完成。 |
| Vector | Partial | Partial | Partial | Partial | ADR 0027 已冻结基础 Canonical 模型；VectorPath、稳定锚点/相对控制柄、Fill Rule、资源上限、Snapshot/Operation、服务重放、WASM 投影、受预算 Rust flatten/bounds、多子路径 fill/stroke hit、stroke mesh、Canvas fallback、SVG/PNG/PDF 导出、可拖拽锚点与控制柄 overlay 和基础 Inspector 均已有候选闭环。`Move`/`Insert`/`DeleteVectorPoint`、`SetVectorSubpathClosed`、`SetVectorPointHandles`、段分割及同/跨 Vector 端点连接覆盖 Core、WASM、协议、Service；Pen 支持连续落点、拖拽镜像柄、闭合、Enter/Esc、端点续画与瞬态下一段预览。复杂自交的 fill tessellation、异构路径 fallback 一致性和独立跨格式/服务验收仍未完成。 |
| BooleanOperation | Partial | Partial | Partial | Partial | ADR 0028 已冻结为保留 children 的活结构：四种 operation、Subtract 的主体顺序、退化输入、派生几何不写 Snapshot 与 Flatten 的可撤销替换语义均已定义。Canonical `NodeKind`/operation enum、Hash、Protobuf Snapshot、Document Service、WASM JSON 投影，以及创建和 selector 变更 Operation 已接通；Core 已允许其承载直接 children，并在事务提交边界拒绝少于两个操作数的空壳/单操作数结构，重设父级、撤销和重做保持原子性。UI 可从两个以上 Vector 图层创建四种 live Boolean，并切换 selector；Rust/WASM 的纯 Rust VectorPath flatten → Union/Intersect/Subtract/Exclude 入口会按每个输入 Fill Rule 归一、忽略开放/退化路径，Canvas、hit test 与 SVG 导出均消费同一派生轮廓。当前 Vector-only UI 可 Flatten 为一个普通 Vector，并以创建替代节点、删除 Boolean 子树和恢复层级位置的一笔 Core 事务提交；Line 的实线、同端帽虚线和装饰端帽，以及 Vector 的同端帽实线/虚线，均可通过同一 Rust tessellation → union 成可编辑填充路径。无法派生（不合法或超预算）的 Boolean 会从 SVG 整体省略且输出明确警告，避免误导性 wrapper/operand 导出；PDF、混合节点、非 Line/Vector 节点、Vector 的异端帽或装饰端帽 Outline Stroke 与复杂 Fixture 仍未实现，不能用一次性 Vector 结果替代。 |
| Mask / Clip | Partial | Partial | Partial | Partial | ADR 0029 冻结唯一的同级后续层 alpha mask；`SetMask` 经过 Core Hash、Undo/Redo、WASM、Protobuf Operation、Service reducer 与 Snapshot 扩展键 round-trip。Canvas 对含 Mask 页使用隔离 surface + `destination-in`，最多两层嵌套、每层 128 MiB，并按深度复用表面；普通点击、框选和选择叠加层同时检查 Frame Clip 与最近的前置 Mask，SVG 使用 `mask-type="alpha"`。Inspector 可启用/解除标记，解除不重排或重写节点；固定夹具已复验图片 Mask。PDF、像素 Golden、像素级 Selection Bounds 裁剪与独立恢复验收仍未完成。 |
| 基础 Drop Shadow | Supported | Partial | Supported | Supported | R3 的单一 Drop Shadow 已覆盖 Canonical Hash、Undo/Redo、Protobuf、Snapshot、Service/WASM、Canvas 和 Inspector。E1 已将其无损投影到最多 8 项的有序 `EffectStack`，并在 Core/Codec/Service/WASM/Worker 保留旧字段与首项一致；Inspector 可添加、编辑、显隐、删除和调整顺序，并可编辑 Inner Shadow、Layer/Background Blur 与六种 Blend Mode。Canvas 为多阴影复用受 128 MiB/张、256 MiB/帧限制的源图、阴影与 scratch 表面，按 Stack 顺序合成后只绘制一次本体；非零 spread 在模糊前以有界、可分离的 SourceAlpha morphology 处理，预算拒绝保留首个兼容阴影并记录诊断。SVG 使用同顺序的 `feMorphology → GaussianBlur → Offset`；PNG/PDF 消费该冻结 SVG。WebGPU 对无 spread 的 1–8 项普通 Drop Shadow 逐项以两张池纹理执行 source、5×5 binomial blur 和 premultiplied composite，再绘制一次本体；含 spread、其它 Effect、Blend、Mask 及预算拒绝仍稳定回退 Canvas。跨渲染器像素 Golden 仍属于 E1。 |
| Slice | Supported | Partial | Partial | Supported | ADR 0030 冻结为无 children、无 Paint 的 Canonical 导出区域；NodeKind、Hash、Snapshot、Operation、Service/WASM、Layers、选择、几何编辑和 Undo/Redo 已接通，Canvas/Hit/普通 SVG 均不会绘制 Slice。选中一个或多个 Slice 后，SVG/透明 PNG/白底 JPEG-PDF 都按冻结的世界矩阵（含旋转）裁出同一范围；PNG 逐张下载，PDF 合为多页。工具栏提供 1×、2×、4×、8× 导出倍率以及 PNG 透明/白底选择，最多 32 个且总计受 16,384px、64MP/256MiB 预算保护。PDF 任意背景、原生矢量与跨格式兼容报告仍未完成。 |
| 完整编辑语义与可访问 UI | Partial | Partial | Partial | N/A | 画布工具快捷键 + Enter 创建、图层方向键、Tab/Shift+Tab 层级、F2 重命名、Backspace/Delete、排序快捷键、Mixed Inspector 和 polite selection live region 已实现；多选 X/Y/W/H 与 Canvas 八手柄共用 world visual bounds 和原子缩放路径，旋转/Relative-v1/Group 子树会安全回写。完整菜单/对话框/提示系统、全部 Inspector NotApplicable 矩阵和屏幕阅读器端到端验收仍在 Phase 2。 |
| 前端目录、UI Primitive 与构建边界 | Partial | N/A | Partial | N/A | App Router、`components.json`、`components/ui`、ESLint 导入限制、依赖 allowlist、Rust 工具链锁定与 CI 已就绪；`services/mock-backend` 已作为独立进程及版本化契约检查存在，Phase 1 建立可独立构建、部署和回滚的 Rust Document/Asset Backend |
| Frame Constraints | Supported | N/A | Partial | Supported | `min/center/max/stretch/scale` 已覆盖 Core、Snapshot、Operation、WASM、Worker、Inspector、Legacy 轴对齐 Frame 与 Relative-v1 子树；单选及排除 Group/Section 的 Mixed Inspector 都可逐轴编辑或移除显式约束。旋转/仿射 Frame Resize 会将直接 Legacy child 及其完整 Legacy Group 子树迁移到 Frame-local Relative-v1，再以同一约束公式计算；连续 Resize 无累计漂移，嵌套 Frame/Section/Boolean 保持其独立布局边界。跨到不支持的父级后，保留的约束会被 Inspector 明确标为 NotApplicable。镜像、Reparent 世界变换和完整 Auto Layout 互斥仍待 L2/L3。 |
| Auto Layout | Partial | Partial | Partial | Partial | ADR 0032 已冻结单轴 Auto Layout Canonical 模型；Core、Snapshot v21、Operation、Codec、Service、WASM、Worker 和 Inspector 支持 Padding、Gap、Alignment、Child Align Self、Hug/Fill、Min/Max、Absolute、固定尺寸 Wrap、三层嵌套和局部祖先 Dirty Set。child `alignSelf` 会被协议编码为独立 `setAutoLayout`，并已在真实浏览器→服务保存→重载中验证 hash 一致。无历史 fixture/旧投影会在完整树装载后执行一次不增加 revision 的确定性回流；已持久化 Snapshot 保持既有几何与 Hash，不会在读取时重算。拖拽重排显示同一 PositionId 语义的插入线与反馈，普通 Constraints 在活跃 Auto Layout 作用域内明确不适用。复杂文本/Wrap 组合、完整 PNG/PDF/SVG 对照与独立验收仍未完成。 |
| Components、Variables、Figma 导入/写回 | Later | Later | Later | Later | Phase 3 |
| 多人协同、评论、权限 | Later | N/A | Later | N/A | Phase 4 |
| `.fig` 私有文件读写 | Unsupported | Unsupported | Unsupported | Unsupported | 明确非目标 |
